#q2 wrong: self.resize(self,self.capacity // 2)
#right: self.resize(self.capacity // 2)
#=> only add self when defined
'''
            # i< len has to be 1st argument, b/c list[len]  = index error

            while (i < len(lst) and not(lst[i] == val)):
'''
